import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fashionstore.app',
  appName: 'FashionStore AR',
  webDir: 'dist/frontend-angular-ar/browser'
};

export default config;
